import Hero from '@/components/ask-for-quote/hero';
import AskForQuoteContent from '@/components/ask-for-quote/askForQuote-content';

const AskForQuote = () => {
  return (
    <div>
      <Hero />
      <AskForQuoteContent />
    </div>
  );
};

export default AskForQuote;
